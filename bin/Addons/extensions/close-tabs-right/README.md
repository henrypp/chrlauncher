# chrlauncher Close Tabs Right

Local Manifest V3 extension loaded by chrlauncher when `ChromiumEnableCloseTabsRightExtension=true`.

It adds `Close tabs to the right` to the tab/action context menu and closes every unpinned tab to the right of the selected tab in the same window.
